# Frontend Configuration for local development
# Copy this file to frontend/.env.local and customize if needed.
# These variables are used by the Next.js application when running locally (e.g., via `npm run dev`).
# For Docker builds, these are passed as build arguments (see Dockerfile.frontend and docker-compose.yml).

# URL of the backend API
# This should point to where your backend server is running.
# Default assumes backend is running on localhost:5001.
NEXT_PUBLIC_API_URL=http://localhost:5001/api

# URL for WebSocket connections
# This should point to your backend server where Socket.IO is running.
# Default assumes backend is running on localhost:5001.
NEXT_PUBLIC_SOCKET_URL=http://localhost:5001