export enum Role {
  USER = "USER",
  ADMIN = "ADMIN"
}

export enum GameStatus {
  PENDING = "PENDING",
  ACTIVE = "ACTIVE",
  ROUND_ENDED = "ROUND_ENDED",
  FINISHED = "FINISHED",
  CANCELLED = "CANCELLED"
}