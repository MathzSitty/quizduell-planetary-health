# DATABASE CONFIGURATION
# For PostgreSQL (recommended for production, adjust user, password, host, port, dbname)
# DATABASE_URL="postgresql://your_user:your_password@localhost:5432/quizduell_db?schema=public"
# For SQLite (easier for local development)
DATABASE_URL="file:./dev.db"

# JWT CONFIGURATION
JWT_SECRET="YOUR_VERY_STRONG_JWT_SECRET_KEY_AT_LEAST_32_CHARACTERS_LONG"
JWT_EXPIRES_IN="7d" # Example: 7 days

# SERVER CONFIGURATION
PORT=5001

# FRONTEND URL (for CORS and Socket.IO)
FRONTEND_URL="http://localhost:3000"

# INITIAL ADMIN USER (for prisma/seed.ts)
INITIAL_ADMIN_EMAIL="admin@example.com"
INITIAL_ADMIN_PASSWORD="SecureAdminPassword123!" # Change this!
INITIAL_ADMIN_NAME="Quiz Admin"
INITIAL_ADMIN_UNIHANDLE="HS-Admin"